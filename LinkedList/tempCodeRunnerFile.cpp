Node* rev = head;
        // int carry = 1;
        // while(rev!=NULL && carry==1){
        //     if(rev->data == 9){
        //         rev->data = rev->data + carry;
        //         carry = 1;
        //     }
        //     else
        //         carry = 0;
        //     rev = rev->next;
        // }

        // head = reverse(rev);